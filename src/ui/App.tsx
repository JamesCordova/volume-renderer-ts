/**
 * Raiz de la capa de UI en React. Vive junto al canvas de Three.js (ver
 * App.ts) pero nunca lo toca -- se comunican por un store compartido, no por
 * props ni por DOM. Ver intern-talk/arquitectura-frontend-react.md.
 *
 * Etapa 1 (andamiaje): todavia no hay nada real montado aca, solo se
 * confirma que React convive con el canvas sin romper nada.
 */
export default function App() {
    return null
}
